# Next Demo Frontend

## Folder Structures

These folder are intensionally placed user root folder to follow the content of
`next`

`pages` - top level pages

`components` - individuals components

`static` - staitc files (images, etc.)
